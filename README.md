# USE UV
# How to Use uv (Python Package Manager)

This project uses **uv** for fast and clean Python dependency management.

---

##  1. Install uv

If you don’t have uv installed:

```bash
pip install uv
```


## 2. Sync
```bash
uv sync
```